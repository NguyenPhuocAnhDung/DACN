# === Core numeric stack (pin NumPy < 2 to avoid binary-compat issues you hit) ===
numpy==1.26.4
scipy==1.11.4
pandas==2.2.2

# === ML stack ===
scikit-learn==1.4.2
imbalanced-learn==0.12.2
lightgbm==4.3.0
joblib==1.4.2

# === Visualization ===
matplotlib==3.8.4
seaborn==0.13.2
tqdm==4.66.4

# === I/O (Parquet/Excel) ===
pyarrow==14.0.2
fastparquet==2024.5.0
openpyxl==3.1.3

# === Optional helpers ===
# (Keep for building wheels if needed on Windows)
wheel>=0.41.2
setuptools>=68.0.0
